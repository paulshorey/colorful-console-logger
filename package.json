{
  "name": "colorful-console-logger",
  "type": "module",
  "version": "0.1.1",
  "description": "Local module, import as a node module path. Install with `npm link ./path/to/here`",
  "private": false,
  "dependencies": {},
  "devDependencies": {
    "parcel": "^2.0.0-beta.1"
  },
  "scripts": {
    "build": "rm -rf .parcel-cache && parcel build ./index.js ./init.js"
  },
  "author": "",
  "license": "ISC",
  "targets": {
    "app": {
      "engines": {
        "browsers": "> 0.25%"
      }
    }
  }
}

{
  "name": "colorful-console-logger",
  "version": "0.9.9",
  "description": "Alpha. Experimental. Download and install as local module. Install with `npm link ./path/to/folder`",
  "private": false,
  "author": "Paul Shorey <paulshorey.com>",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "jest-coverage-badges": "^1.1.2",
    "jest": "^26.6.3",
    "eslint": "^7.17.0",
    "husky": "^4.3.7",
    "parcel": "^1.10.3"
  },
  "scripts": {
    "build": "rm -rf .parcel-cache && parcel build ./src/cconsole*.js",
    "lint": "eslint src --max-warnings=0",
    "dev": "npm run build -- --watch",
    "test": "jest --coverage && jest-coverage-badges",
    "test:watch": "jest --watch --verbose",
    "publish": "npm version patch && git add . && npm commit && npm push && npm publish"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run lint && npm run test && npm run build && git add ."
    }
  },
  "husky-hooks-alt":{
    "post-commit": "echo '\n\nPUSHING to remote' && git push"
  },
  "targets": {
    "app": {
      "engines": {
        "browsers": "> 0.75%"
      }
    }
  },
  "type": "commonjs",
  "main": "./dist/cconsole.js",
  "module": "./dist/cconsole.js"
}
